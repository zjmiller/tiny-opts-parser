{
  "name": "tiny-opts-parser",
  "version": "0.0.1",
  "description": "A tiny options parser.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "babel src/index.js -o dist/index.js && uglifyjs dist/index.js --compress --mangle -o dist/index.js",
    "prepublish": "npm run build",
    "test": "mocha",
    "watch": "babel src --watch -d dist"
  },
  "author": "Zachary Miller",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "mocha": "^3.1.2",
    "uglify-js": "^2.7.4"
  }
}
